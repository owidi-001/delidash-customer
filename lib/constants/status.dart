enum ServiceStatus {
  initial,
  loading,
  loadingSuccess,
  loadingFailure,
  submitting,
  submissionSuccess,
  submissionFailure
}

enum AuthenticationStatus { unknown, authenticated, unAuthenticated }
